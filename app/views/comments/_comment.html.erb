<div class="card mb-4 shadow-sm dist_posts raz" style="border-radius:1.25rem; max-width: 370px;">
  <div class="card-body">
    <p class="card-text" style="font-size: 1rem"><%= comment.body %></p>
    <div class="d-flex justify-content-between align-items-center">
        <% if user_signed_in? %>
          <p class="card-text" style="font-size: 1.25rem;  text-align: right; padding-right: 1rem"><%= current_user.username %></p>
        <%end %>
    </div>
  </div>
</div>
